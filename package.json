{
  "name": "threetsdemo",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "vite preview",
    "deploy": "tsc && vite build && gh-pages -d ./dist/"
  },
  "devDependencies": {
    "@types/node": "^16.11.6",
    "@types/three": "^0.130.1",
    "three": "^0.131.1"
  },
  "dependencies": {
    "gh-pages": "^3.2.3",
    "vite": "^2.4.4"
  }
}
